---
title: 写给工程师的 PRD
description: AI 产品需求文档的结构模板、必写的技术约束、让工程师一看就能动手的写法
---

# 写给工程师的 PRD

> 好的 PRD 不是写给老板看的汇报，而是写给工程师用的行动指南。

---

## AI 产品 PRD 的特殊要求

传统 PRD 的问题：只说"做什么"，不说"怎么判断做好了"。  
AI 产品额外需要：

1. **模型选型约束**：用什么模型、允许多大的成本
2. **输入/输出规格**：精确定义用户输入格式 + 期望的 AI 输出结构
3. **质量标准**：什么叫"回答好"，用什么指标衡量
4. **降级方案**：AI 不可用/回答差时，产品如何兜底
5. **Prompt 草稿**：PM 写初稿，工程师优化（而不是让工程师从零猜）

---

## AI 功能 PRD 模板

### 一、背景与目标

```markdown
## 背景
[用 1-2 句话说清楚：为什么要做这个功能？当前用户的什么痛点？]

## 目标
- 用户目标：[用户通过这个功能能做到什么]
- 业务目标：[对应什么业务指标，如留存率 / 完成率 / 收入]
- 不做什么：[明确排除的范围，防止 scope creep]
```

### 二、功能描述

```markdown
## 用户旅程
1. 用户进入 [页面/入口]
2. 触发条件：[用户做了什么操作]
3. AI 处理：[调用什么能力，预期耗时]
4. 呈现结果：[以什么形式展示给用户]
5. 后续操作：[用户可以对结果做什么]

## 输入规格
- 最大长度：[X Token / X 字]
- 支持格式：[纯文本 / Markdown / 带附件]
- 必填/选填字段：[列清楚]

## 输出规格
- 格式：[纯文本 / Markdown / 结构化 JSON]
- 最大长度：[限制或不限制]
- 必须包含的内容：[如"必须包含来源引用"]
- 不能包含的内容：[如"不能包含品牌名称"]
```

### 三、技术约束（⚠️ AI 产品必填）

```markdown
## 模型要求
- 推荐模型：[如 Claude 3.5 Sonnet / GPT-4o-mini]
- 备选模型：[如主模型不可用时]
- 禁用模型：[如某些合规要求禁用特定供应商]

## 性能要求
- 首字响应时间（TTFT）：< X 秒
- 完整响应时间：< X 秒
- 并发量：峰值 X QPS

## 成本约束
- 单次调用 Token 上限：[输入 X + 输出 Y]
- 月度 API 成本预算：[￥X / $X]
- 超出预算时的降级策略：[切换小模型 / 限流 / 关闭功能]

## 上下文长度
- 需要保留多少轮历史对话：[X 轮 / X Token]
- 超出时如何截断：[删最早 / 压缩摘要 / 提示用户]
```

### 四、Prompt 草稿（⚠️ PM 必须提供）

```markdown
## System Prompt 草稿
你是一个 [角色描述]。你的任务是 [核心任务]。

规则：
1. [必须遵守的规则]
2. [输出格式要求]
3. [边界情况处理]

## 输出格式示例
[给出 1-2 个期望的输出示例，越具体越好]

## 反面示例（不希望输出的）
[给出 1-2 个不希望出现的输出，说明原因]
```

### 五、质量标准与评估

```markdown
## 成功标准
| 指标 | 当前基线 | 目标值 | 衡量方法 |
|------|---------|--------|---------|
| 用户满意度（👍 率） | - | > 80% | 功能内反馈按钮 |
| 完成率（生成结果不为空） | - | > 95% | 后端日志 |
| 平均响应时间 | - | < 5s | APM 监控 |

## AB 测试方案
- 实验组：[新功能]
- 对照组：[现有体验 / 不做 AI]
- 流量比例：[10% / 50%]
- 观测周期：[2 周]
- 成功判定：[指标 A 提升 X%，指标 B 不下降]
```

### 六、降级与边界处理

```markdown
## 降级方案
| 场景 | 触发条件 | 处理方式 |
|------|---------|---------|
| API 不可用 | 连续失败 3 次 | 展示静态兜底文案，报警 |
| 响应超时 | > 30s | 提示"生成失败，请重试" |
| 内容安全拦截 | 敏感词触发 | 提示"无法处理此请求" |
| Token 超限 | 输入过长 | 提示用户缩短输入 |
| 质量过差 | 置信度 < X | 展示"AI 不确定，建议人工确认" |
```

---

## 反面案例：让工程师抓狂的 PRD 写法

### ❌ 模糊的质量要求

> "AI 回答要准确、有用、符合用户期望"

工程师看到这句话：不知道怎么测试、不知道上线标准是什么。

✅ 改为：
> "对于用户输入的产品相关问题，AI 回答的正确率（与人工标注答案对比）> 85%，通过 100 道测试集验证后上线"

### ❌ 没有输出格式规定

> "AI 生成一份简历优化建议"

工程师实现后发现：有时候输出是 Markdown，有时候是纯文本，前端展示一团乱。

✅ 改为：
> "输出固定为 Markdown 格式，包含以下 3 个段落：**整体评价**（3 句话）、**改进建议**（3-5 条 bullet）、**修改示例**（1 个完整段落的修改前后对比）"

### ❌ 让工程师猜 Prompt

> "做一个 AI 面试官，能模拟真实面试场景"

工程师：面试官是什么类型的？问什么问题？怎么追问？评分标准是什么？

✅ 改为：提供完整 System Prompt 草稿 + 3 个对话示例 + 明确说明几种面试官风格的差异

### ❌ 没有降级方案

> "API 调用失败的情况工程师处理"

上线后 API 挂了，工程师不知道展示什么，临时写了一行"系统错误"，用户投诉率飙升。

✅ 改为：在 PRD 里明确每种失败场景的用户侧文案和处理逻辑

---

## 快速参考：AI 功能 PRD Checklist

```
□ 背景和目标（包含不做什么）
□ 完整用户旅程
□ 输入规格（格式、长度限制）
□ 输出规格（格式、长度、必含/禁含内容）
□ 模型选型（首选 + 备选）
□ 性能要求（TTFT、完整响应时间）
□ 成本约束（Token 上限、月度预算）
□ System Prompt 草稿
□ 输出示例（正面 + 反面）
□ 质量指标和评估方案
□ AB 测试设计
□ 完整降级方案
```

---

## 延伸阅读

- [技术方案评审](./02-tech-review)
- [读懂代码](../A-coding/01-read-code)
- [Agent 评估体系](../B-ai-tech/03-agent-evaluation)
